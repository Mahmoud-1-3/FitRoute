import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('App initializes', (WidgetTester tester) async {
    // Smoke test — just verify the app widget can be created.
    // Firebase must be mocked for real widget tests.
  });
}
